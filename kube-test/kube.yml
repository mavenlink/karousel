---
apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    ttl: "60"
spec:
  containers:
  - name: nginx
    image: nginx
---
apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: ClusterIP
  ports:
  - port: 3002
    protocol: TCP
  selector:
    name: foo

